# EmotionLens — pip requirements for a CPU-only production VPS (no conda, no CUDA).
# Install with:
#   pip install -r deploy/requirements-cpu.txt

# Core ML / Computer Vision
numpy>=1.24
scipy>=1.11
opencv-python-headless>=4.8   # headless: no GUI/X11 deps needed on a server
pillow>=10.0
scikit-learn>=1.3

# Web Framework
fastapi>=0.104
uvicorn[standard]>=0.24
gunicorn>=21.2
python-multipart>=0.0.6
websockets>=12.0

# MediaPipe (face detection)
mediapipe>=0.10.8

# Database
sqlalchemy>=2.0
aiosqlite>=0.19
asyncpg>=0.29                 # PostgreSQL driver for production

# PDF Report Generation
reportlab>=4.0
weasyprint>=60.0

# Utilities
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
aiofiles>=23.2

# PyTorch — CPU-only wheels (no CUDA download, much smaller/faster install)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1
torchvision>=0.16
